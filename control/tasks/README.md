# Tasks

The controller's live queue is stored under its configured state directory. This directory holds reviewed task templates and portable task submissions; submit JSON files with `andromeda-submit` or `python3 -m andromeda_control.cli_submit`.
