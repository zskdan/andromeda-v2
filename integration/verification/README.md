# Integration verification

Store reproducible, requirement-linked verification summaries here. Raw or large tool outputs should live in an artifact store and be referenced by immutable identifier or checksum.
